
spring:
    datasource:
        url: 'jdbc:mysql://localhost:3306/vollmed_api'
        username: root
        password: password
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        show-sql: true
        properties:
            hibernate:
                format-sql: true